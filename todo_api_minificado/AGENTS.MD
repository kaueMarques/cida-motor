# 🤖 AGENTS.MD

Este projeto está otimizado para agentes via **Minificação Extrema**.

### Workflow:
1. **Leitura:** Consulte este diretório para entender o comportamento puro (código minificado).
2. **Mapeamento:** Use `dict.tkcd` para descompactar variáveis (`Z1` -> `NomeReal`).
3. **Execução/Edição:** Realize edições **apenas** no diretório original:
   `/home/kaue/_projects/ai_compiler/todo_api`

⚠️ **NUNCA** edite arquivos nesta pasta de minificação. Eles são *read-only* para otimização de contexto.
